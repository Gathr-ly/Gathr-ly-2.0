<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/border" >

    <Button
        android:id="@+id/btOverlap"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="See overlapping schedules"
        app:layout_constraintEnd_toStartOf="@+id/btEditSchedule"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="5dp"
        android:lines="2"
        android:onClick="showOverlaps"/>

    <Button
        android:id="@+id/btEditSchedule"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Edit your schedule"
        app:layout_constraintStart_toEndOf="@+id/btOverlap"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginStart="5dp"
        android:layout_marginEnd="10dp"
        android:lines="2"
        android:onClick="showSchedule"/>

</androidx.constraintlayout.widget.ConstraintLayout>